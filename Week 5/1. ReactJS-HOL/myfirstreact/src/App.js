import './App.css';

// App is a simple functional component. It returns JSX (the markup that
// React converts into virtual DOM nodes and then renders to the real DOM).
function App() {
  return (
    <div className="App">
      <h1>welcome to the first session of React</h1>
    </div>
  );
}

export default App;
